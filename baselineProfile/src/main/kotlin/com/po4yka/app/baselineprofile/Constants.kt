package com.po4yka.app.baselineprofile

/** Application ID of `:androidApp` — the module under test. */
const val APP_PACKAGE_NAME = "com.po4yka.app"

/** How long to wait for a scrollable surface to appear, in milliseconds. */
const val UI_WAIT_TIMEOUT_MS = 5_000L

/** Fraction of display width reserved as gesture margin to avoid system insets. */
const val GESTURE_MARGIN_DIVISOR = 5
